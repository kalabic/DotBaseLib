# `DotBase.Tools`

General-purpose helpers for generic types, task coordination, synchronization, and invalid-operation handling.

Main API: `GenericType<T>`, `GenericTools`, `TaskTools`, `InterlockedExtension`, and `InvalidOperationExtension`.

[Namespace index](../../../README.md#namespaces)
